using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Assignment : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Master The Blaster");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
